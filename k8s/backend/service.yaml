apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: stud-reg-system
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
  sessionAffinity: None
